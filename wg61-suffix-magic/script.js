const wordData = {
  "G1": [
    {
      "word": "care",
      "answer": ["careful", "carefully", "careless", "carelessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "careful": "She was careful with the glass.",
        "carefully": "She carefully held the glass.",
        "careless": "The careless girl broke the glass.",
        "carelessly": "She carelessly dropped the glass."
      }
    },
    {
      "word": "hope",
      "answer": ["hopeful", "hopefully", "hopeless", "hopelessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "hopeful": "He felt hopeful about the test results.",
        "hopefully": "He hopefully waited for the results.",
        "hopeless": "The lost child looked hopeless.",
        "hopelessly": "He was hopelessly lost in the forest."
      }
    },
    {
      "word": "help",
      "answer": ["helpful", "helpfully", "helpless", "helplessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "helpful": "The helpful student carried the teacher’s bags.",
        "helpfully": "The student helpfully opened the door.",
        "helpless": "The helpless kitten was stuck in the tree.",
        "helplessly": "The trapped kitten mewed helplessly."
      }
    },
    {
      "word": "fear",
      "answer": ["fearful", "fearfully", "fearless", "fearlessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "fearful": "The fearful child hid under the bed.",
        "fearfully": "The child fearfully looked at the spider.",
        "fearless": "The fearless firefighter ran into the building.",
        "fearlessly": "The firefighter ran fearlessly into the building."
      }
    },
    {
      "word": "power",
      "answer": ["powerful", "powerfully", "powerless", "powerlessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "powerful": "The elephant is a powerful animal.",
        "powerfully": "The elephant powerfully pushed the tree.",
        "powerless": "We felt powerless during the storm.",
        "powerlessly": "We watched powerlessly as the storm blew our clothes away."
      }
    },
    {
      "word": "pain",
      "answer": ["painful", "painfully", "painless", "painlessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "painful": "The fall was very painful.",
        "painfully": "He groaned painfully after the fall.",
        "painless": "The injection was quick and painless.",
        "painlessly": "The doctor painlessly gave the injection."
      }
    },
    {
      "word": "harm",
      "answer": ["harmful", "harmfully", "harmless", "harmlessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "harmful": "The smoke rising from the factories is harmful.",
        "harmfully": "The smoke spread harmfully through the town.",
        "harmless": "The little spider is harmless.",
        "harmlessly": "The little spider went harmlessly past me."
      }
    },
    {
      "word": "thank",
      "answer": ["thankful", "thankfully", "thankless", "thanklessly"],
      "suffixes": {
        "correct": ["ful", "ly", "less"],
        "incorrect": ["ish", "able", "y"]
      },
      "sentences": {
        "thankful": "I am thankful the rain stopped before the picnic.",
        "thankfully": "Thankfully, the rain stopped before the picnic.",
        "thankless": "Cleaning is often a thankless job.",
        "thanklessly": "The cleaner worked thanklessly all day."
      }
    }
  ],
  "G2": [
    {
      "word": "cheer",
      "answer": ["cheerful", "cheerfully", "cheerless", "cheerlessly", "cheery"],
      "suffixes": {
        "correct": ["ful", "ly", "less", "y"],
        "incorrect": ["ish", "able"]
      },
      "sentences": {
        "cheerful": "The cheerful girl smiled at everyone.",
        "cheerfully": "She cheerfully greeted her friends.",
        "cheerless": "The boy had a cheerless look on his face.",
        "cheerlessly": "The boy cheerlessly stared out the window.",
        "cheery": "Grandpa has a cheery voice."
      }
    }
  ],
  "G3": [
    {
      "word": "use",
      "answer": ["useful", "usefully", "useless", "uselessly", "usable"],
      "suffixes": {
        "correct": ["ful", "ly", "less", "able"],
        "incorrect": ["ish", "y"]
      },
      "spelling_alert": "USABLE is more common than USEABLE.",
      "sentences": {
        "useful": "A pencil is a useful tool.",
        "usefully": "The children spent their time usefully.",
        "useless": "A broken pen is useless.",
        "uselessly": "The broken pen lay uselessly on the shelf.",
        "usable": "The old chair is still usable."
      }
    },
    {
      "word": "colour",
      "answer": ["colourful", "colourfully", "colourless", "colourlessly", "colourable"],
      "suffixes": {
        "correct": ["ful", "ly", "less", "able"],
        "incorrect": ["ish", "y"]
      },
      "sentences": {
        "colourful": "The artist made a colourful mural.",
        "colourfully": "The artist painted the wall colourfully.",
        "colourless": "The sky is colourless today.",
        "colourlessly": "The sky is colourlessly grey today.",
        "colourable": "The picture was easily colourable for young children."
      }
    }
  ],
  "G4": [
    {
      "word": "play",
      "answer": ["playful", "playfully", "playable"],
      "suffixes": {
        "correct": ["ful", "ly", "able"],
        "incorrect": ["less", "ish", "y"]
      },
      "sentences": {
        "playful": "The playful puppy chased its tail.",
        "playfully": "The puppy playfully chased its tail.",
        "playable": "The old piano is still playable."
      }
    },
    {
      "word": "watch",
      "answer": ["watchful", "watchfully", "watchable"],
      "suffixes": {
        "correct": ["ful", "ly", "able"],
        "incorrect": ["less", "ish", "y"]
      },
      "sentences": {
        "watchful": "The watchful mother kept her eyes on her baby.",
        "watchfully": "The mother sat watchfully on the bench as her baby played.",
        "watchable": "The movie was fun and watchable."
      }
    }
  ],
  "G5": [
    {
      "word": "child",
      "answer": ["childish", "childishly", "childless"],
      "suffixes": {
        "correct": ["ly", "less", "ish"],
        "incorrect": ["ful", "able", "y"]
      },
      "sentences": {
        "childish": "Crying over a toy is childish.",
        "childishly": "He childishly refused to share.",
        "childless": "The childless couple adopted a puppy."
      }
    },
    {
      "word": "self",
      "answer": ["selfish", "selfishly", "selfless", "selflessly"],
      "suffixes": {
        "correct": ["ly", "less", "ish"],
        "incorrect": ["ful", "able", "y"]
      },
      "sentences": {
        "selfish": "It is selfish to take all the sweets.",
        "selfishly": "She selfishly took all the sweets.",
        "selfless": "The selfless man helped everyone.",
        "selflessly": "The man helped everyone selflessly."
      }
    }
  ],
  "G7": [
    {
      "word": "friend",
      "answer": ["friendly", "friendless"],
      "suffixes": {
        "correct": ["ly", "less"],
        "incorrect": ["ful", "ish", "able", "y"]
      },
      "sentences": {
        "friendly": "The new student was very friendly.",
        "friendless": "The friendless boy sat alone at lunch."
      }
    },
    {
      "word": "speech",
      "answer": ["speechless", "speechlessly"],
      "suffixes": {
        "correct": ["ly", "less"],
        "incorrect": ["ful", "ish", "able", "y"]
      },
      "sentences": {
        "speechless": "She was speechless when she won the prize.",
        "speechlessly": "She stared speechlessly when she won the prize."
      }
    },
    {
      "word": "effort",
      "answer": ["effortless", "effortlessly"],
      "suffixes": {
        "correct": ["ly", "less"],
        "incorrect": ["ful", "ish", "able", "y"]
      },
      "sentences": {
        "effortless": "The dancer made it look effortless.",
        "effortlessly": "The dancer glided effortlessly across the stage."
      }
    },
    {
      "word": "end",
      "answer": ["endless", "endlessly"],
      "suffixes": {
        "correct": ["ly", "less"],
        "incorrect": ["ful", "ish", "able", "y"]
      },
      "sentences": {
        "endless": "The summer holidays felt endless.",
        "endlessly": "The baby cried endlessly on the plane."
      }
    }
  ],
  "G8": [
    {
      "word": "spot",
      "answer": ["spotless", "spotlessly", "spotty"],
      "suffixes": {
        "correct": ["ly", "less", "y"],
        "incorrect": ["ful", "ish", "able"]
      },
      "spelling_alert": "SPOTTY not SPOTY",
      "sentences": {
        "spotless": "The kitchen was spotless.",
        "spotlessly": "The kitchen was spotlessly clean.",
        "spotty": "The spotty dog had black and white fur."
      }
    },
    {
      "word": "fault",
      "answer": ["faultless", "faultlessly", "faulty"],
      "suffixes": {
        "correct": ["ly", "less", "y"],
        "incorrect": ["ful", "ish", "able"]
      },
      "sentences": {
        "faultless": "Her singing was faultless.",
        "faultlessly": "She sang faultlessly.",
        "faulty": "The faulty wire caused a spark."
      }
    },
    {
      "word": "noise",
      "answer": ["noiseless", "noiselessly", "noisy"],
      "suffixes": {
        "correct": ["ly", "less", "y"],
        "incorrect": ["ful", "ish", "able"]
      },
      "spelling_alert": "NOISY not NOISEY",
      "sentences": {
        "noiseless": "The cat moved in a noiseless way.",
        "noiselessly": "The cat jumped and landed noiselessly on her feet.",
        "noisy": "The noisy birds woke me up."
      }
    },
    {
      "word": "sleep",
      "answer": ["sleepless", "sleeplessly", "sleepy"],
      "suffixes": {
        "correct": ["ly", "less", "y"],
        "incorrect": ["ful", "ish", "able"]
      },
      "sentences": {
        "sleepless": "He had a sleepless night before the exam.",
        "sleeplessly": "He tossed sleeplessly in his bed.",
        "sleepy": "The sleepy child rubbed his eyes."
      }
    },
    {
      "word": "home",
      "answer": ["homely", "homeless", "homelessly", "homey"],
      "suffixes": {
        "correct": ["ly", "less", "y"],
        "incorrect": ["ful", "ish", "able"]
      },
      "sentences": {
        "homely": "The cottage had a homely feel.",
        "homeless": "The homeless dog needed a family.",
        "homelessly": "The dog wandered homelessly through the streets.",
        "homey": "The warm kitchen felt homey."
      }
    }
  ],
  "G9": [
    {
      "word": "love",
      "answer": ["loveless", "lovelessly", "lovable"],
      "suffixes": {
        "correct": ["ly", "less", "able"],
        "incorrect": ["ful", "ish", "y"]
      },
      "spelling_alert": "LOVABLE is more common than LOVEABLE",
      "sentences": {
        "loveless": "The loveless puppy sat in a corner at the shelter.",
        "lovelessly": "He spoke lovelessly about his old school.",
        "lovable": "The lovable puppy wagged its tail."
      }
    },
    {
      "word": "count",
      "answer": ["countless", "countlessly", "countable"],
      "suffixes": {
        "correct": ["ly", "less", "able"],
        "incorrect": ["ful", "ish", "y"]
      },
      "sentences": {
        "countless": "There are countless stars in the sky.",
        "countlessly": "The man apologised countlessly for his mistake.",
        "countable": "The apples in the basket are countable."
      }
    }
  ],
  "G10": [
    {
      "word": "fever",
      "answer": ["feverish", "feverishly"],
      "suffixes": {
        "correct": ["ly", "ish"],
        "incorrect": ["ful", "less", "able", "y"]
      },
      "sentences": {
        "feverish": "The sick child felt feverish.",
        "feverishly": "The sick child called feverishly out to her parents."
      }
    },
    {
      "word": "clown",
      "answer": ["clownish", "clownishly"],
      "suffixes": {
        "correct": ["ly", "ish"],
        "incorrect": ["ful", "less", "able", "y"]
      },
      "sentences": {
        "clownish": "His clownish behaviour made everyone laugh.",
        "clownishly": "He clownishly danced around the room."
      }
    }
  ],
  "G11": [
    {
      "word": "value",
      "answer": ["valueless", "valuable"],
      "suffixes": {
        "correct": ["less", "able"],
        "incorrect": ["ful", "ish", "y", "ly"]
      },
      "spelling_alert": "VALUABLE not VALUEABLE",
      "sentences": {
        "valueless": "The broken toy was valueless.",
        "valuable": "The ring is very valuable."
      }
    }
  ]
}

const wordSpan = document.getElementById('word-span');
const suffix1 = document.getElementById('suffix1');
const suffix2 = document.getElementById('suffix2');
const suffix3 = document.getElementById('suffix3');
const suffix4 = document.getElementById('suffix4');
const suffix5 = document.getElementById('suffix5');
const suffix6 = document.getElementById('suffix6');
const finalWord = document.getElementById('final-word');
const lottieWord = document.getElementById('lottie-word');
const exampleSentence = document.getElementById('exampleSentence');
const showExample = document.getElementById('showExampleBtn');

let wordObj = null;
let wordIndex = 0;


const IMAGES = [
  'Assets/tree.svg',
  'Assets/milestone.svg',
  'Assets/mountain.svg',
  'Assets/stone.svg'
];

//Playing Lotties

function playCorrectAnswerLottie(objectName) {

  const containerId = `${objectName}-${wordIndex}`;
  const container = document.getElementById(containerId);

  if (!container) {
    console.warn(`Container ${containerId} not found`);
    return;
  }

  const variant = (wordIndex % 2 === 0) ? '01' : '02';

  // 3. Construct the path (e.g., "./Assets/JSON/tree_01.json")
  const animationPath = `./Assets/Images/JSON/${objectName}_${variant}.json`;

  // Clear previous SVG if any
  container.innerHTML = '';
  // Make sure the container is visible (some containers may be hidden by default)
  container.style.display = 'block';

  const anim = lottie.loadAnimation({
    container: container,
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: animationPath,
    rendererSettings: {
      hideOnTransparent: false, // Prevents disappearing if frames are empty
      preserveAspectRatio: 'xMidYMid meet'
    }
  });

  // Force it to stay on the last frame
  anim.addEventListener('complete', () => {
    anim.goToAndStop(anim.totalFrames - 1, true);
  });
}

function resetLotties() {
  // Ensure wordObj exists before trying to extract the image
  if (!wordObj || !wordObj.image) return;

  // 1. Extract the name (e.g., "mountain")
  const objectName = wordObj.image.match(/\/([^/]+)\./)[1];

  const slot = document.getElementById(`${objectName}Word`);
  const wordDiv = document.getElementById(`${objectName}Main`);
  if (slot || wordDiv) {
    slot.textContent = "";
    slot.style.display = "none";
    wordDiv.style.display = "none";
  }

  // 2. Loop through and clear the specific IDs for that object
  // Changed i to start at 0 and go to 5 (adjust if you have more leaves)
  for (let i = 0; i <= 4; i++) {
    const leafId = `${objectName}-${i}`;
    const objectContainer = document.getElementById(leafId);
    if (objectContainer) {
      objectContainer.innerHTML = '';
    }
  }
}

/**
 * DATA LAYER: Manages the words and ensures no repeats
 */
class WordPicker {
  constructor(data) {
    this.data = data;
    this.history = new Set();
    this.allWords = this.flattenData();

    this.images = IMAGES;
    this.imageIndex = 0;
  }

  flattenData() {
    let flatList = [];
    for (const groupKey in this.data) {
      this.data[groupKey].forEach((item, index) => {
        flatList.push({ group: groupKey, root: item.word, details: item });
      });
    }
    return flatList;
  }

  getNext() {
    const available = this.allWords.filter(w => !this.history.has(`${w.group}-${w.root}`));

    if (available.length === 0) {
      this.history.clear();
      return this.getNext();
    }

    const selected = available[Math.floor(Math.random() * available.length)];
    this.history.add(`${selected.group}-${selected.root}`);

    // 🔁 rotate image (1 → 2 → 3 → 4 → 1 ...)
    const image = this.images[this.imageIndex];
    this.imageIndex = (this.imageIndex + 1) % this.images.length;

    return {
      ...selected,
      image
    };
  }
}

function handleSmoothMove(source, target) {
  if (!source || !target) return;
  if (target.dataset.full === "true") return;

  target.dataset.full = "true";
  target.style.display = "block";

  source.style.opacity = "0.3";
  source.style.pointerEvents = "none";
  // source.style.filter = "blur(3px)"; // Permanently disable and blur

  const clone = source.cloneNode(true);
  clone.removeAttribute("id");

  clone.style.position = "absolute";
  clone.style.top = "0";
  clone.style.left = "0";
  clone.style.opacity = "1";
  clone.style.filter = "none"; // Remove blur for the animated piece
  clone.style.pointerEvents = "none";
  clone.style.fontSize = "32px";
  clone.style.fontWeight = "bold";

  target.appendChild(clone);
}

function showExampleSentences(wordObj) {
  const popup = document.getElementById('examplePopup');
  const list = document.getElementById('exampleSentenceList');

  if (!popup || !list) return;

  list.innerHTML = "";

  const sentencesObj = wordObj.details.sentences || {};

  Object.entries(sentencesObj).forEach(([key, sentence]) => {
    const li = document.createElement('li');

    // Create regex to bold the key inside sentence (case-insensitive)
    const regex = new RegExp(`(${key})`, 'gi');

    // Replace key with bold version
    li.innerHTML = sentence.replace(regex, '<strong>$1</strong>');

    list.appendChild(li);
  });

  popup.setAttribute('aria-hidden', 'false');
}

function showAllAnswers(wordObj) {
  if (!wordObj || !wordObj.details || !Array.isArray(wordObj.details.answer)) return;

  const imgEl = document.getElementById('objects-img');

  const answers = wordObj.details.answer;
  const objectName = wordObj.image.match(/\/([^/]+)\./)[1];

  const assetMap = {
    tree: './Assets/tree-1.svg',
    milestone: './Assets/milestone-1.svg',
    mountain: './Assets/mountain-1.svg',
    stone: './Assets/stone-1.svg'
  };

  // If the object exists in our map, update the source
  if (assetMap[objectName]) {
    imgEl.src = assetMap[objectName];
  }
  // 1. Reset standard word slots (word1, word2, etc)
  const wordSlots = [
    document.getElementById('word1'),
    document.getElementById('word2'),
    document.getElementById('word3'),
    document.getElementById('word4'),
    document.getElementById('word5')
  ];

  wordSlots.forEach(slot => {
    if (!slot) return;
    slot.textContent = "";
    slot.style.display = "none";
  });

  // 2. Mapping for the visual group containers
  const mapping = {
    tree: { container: 'treeWords', prefix: 'treeWord' },
    milestone: { container: 'milestoneWords', prefix: 'milestoneWord' },
    mountain: { container: 'mountainWords', prefix: 'mountainWord' },
    stone: { container: 'stoneObjects', prefix: 'stoneWord' }
  };

  const map = mapping[objectName];

  // Show the main group container (e.g., the tree SVG group)
  if (map) {
    const groupEl = document.getElementById(map.container);
    if (groupEl) groupEl.style.display = 'block';
  }

  // 3. Fill slots and visual elements based on answers
  answers.forEach((answer, index) => {
    const cleanAnswer = answer.trim();

    // Update standard list slots
    if (wordSlots[index]) {
      wordSlots[index].textContent = cleanAnswer;
      wordSlots[index].style.display = "block";
    }

    // Update visual group slots (foreignObjects)
    if (map) {
      const slotId = `${map.prefix}${index + 1}`;
      const fo = document.getElementById(slotId);
      if (fo) {
        fo.style.display = 'block';
        const span = fo.querySelector('span');
        if (span) span.textContent = cleanAnswer;
      }

      // Show associated lottie/leaf containers if they exist
      const lottieContainer = document.getElementById(`${objectName}-${index}`);
      if (lottieContainer) lottieContainer.style.display = 'block';
    }
  });

  // 4. Disable suffix interaction since answers are revealed
  document.querySelectorAll("#list-suffix li").forEach(li => {
    li.style.pointerEvents = "none";
    li.style.opacity = "0.3";
  });

  // Show the example button as the round is effectively over
  if (typeof showExample !== 'undefined') {
    showExample.style.display = 'block';
  }
}


document.addEventListener("DOMContentLoaded", () => {
  const picker = new WordPicker(wordData);

  const slots = [
    document.getElementById("puzzleSuffix1"),
    document.getElementById("puzzleSuffix2")
  ];

  const sourceList = document.getElementById("list-suffix");
  const wordSpan = document.getElementById("word-span");
  const nextBtn = document.getElementById("next-btn");
  const showAnswerBtn = document.getElementById('showAnswer');

  // const finalWord = document.getElementById("final-Word");

  let correctSuffixes = [];
  let answers = [];
  let selectedSuffixes = [];
  let completedAnswers = [];

  const wordSlots = [
    document.getElementById('word1'),
    document.getElementById('word2'),
    document.getElementById('word3'),
    document.getElementById('word4'),
    document.getElementById('word5')
  ];

  if (showAnswerBtn) {
    showAnswerBtn.addEventListener('click', () => {
      showAllAnswers(wordObj); // current wordObj
    });
  }

  finalWord.style.display = "none";

  function resetSlots() {
    slots.forEach(slot => {
      slot.style.display = "none";
      slot.dataset.full = "false";
      const clone = slot.querySelector(".centerTxt-wrap");
      if (clone) clone.remove();
    });
  }

  function loadNextWord() {

    resetLotties();

    wordObj = picker.getNext();
    const d = wordObj.details;

    // currentWordObj = wordObj;

    const imgEl = document.getElementById('objects-img');
    imgEl.src = wordObj.image;

    correctSuffixes = d.suffixes.correct.map(s => s.trim());
    answers = d.answer.map(a => a.trim());
    selectedSuffixes = [];
    completedAnswers = [];
    wordIndex = 0;

    finalWord.style.display = "none";
    exampleSentence.style.display = 'none';
    showExample.style.display = 'none';
    // reset word slots
    wordSlots.forEach(ws => {
      if (ws) ws.textContent = "";
    });

    // reset slots
    slots.forEach(slot => {
      slot.style.display = "none";
      slot.dataset.full = "false";
      const clone = slot.querySelector(".centerTxt-wrap");
      if (clone) clone.remove();
    });

    // reset suffix buttons
    document.querySelectorAll("#list-suffix li").forEach(li => {
      li.style.opacity = "1";
      li.style.pointerEvents = "auto";
      li.style.filter = "none";
    });

    const objectName = wordObj.image.match(/\/([^/]+)\./)[1];

    wordSpan.textContent = wordObj.root;
    // hide/reset all group word containers (tree, milestone, mountain, stone)
    const groupPrefixes = ['treeWord', 'milestoneWord', 'mountainWord', 'stoneWord'];
    const groupContainers = ['treeWords', 'milestoneWords', 'mountainWords', 'stoneObjects'];
    groupContainers.forEach(gc => {
      const el = document.getElementById(gc);
      if (el) el.style.display = 'none';
    });
    // hide and clear individual foreignObject slots
    for (let i = 1; i <= 4; i++) {
      groupPrefixes.forEach(prefix => {
        const fo = document.getElementById(`${prefix}${i}`);
        if (fo) {
          fo.style.display = 'none';
          const span = fo.querySelector('span');
          if (span) span.textContent = '';
        }
      });
    }
    //  `${objectName}Word`.textContent = wordObj.root;

    const slot = document.getElementById(`${objectName}Word`);
    const wordDiv = document.getElementById(`${objectName}Main`);
    if (slot || wordDiv) {
      slot.textContent = wordObj.root;
      slot.style.display = "block";
      wordDiv.style.display = "block";
    }

    const allSuffixes = [...d.suffixes.correct, ...d.suffixes.incorrect]
      .sort(() => Math.random() - 0.5);

    for (let i = 1; i <= 6; i++) {
      const el = document.getElementById(`puzzle${i}`);
      if (!el) continue;

      const span = el.querySelector("span");
      span.textContent = allSuffixes[i - 1] || "";
      span.style.fontSize = "40px";   // set font size here
      // el.querySelector("span").textContent = allSuffixes[i - 1] || "";
      el.style.display = allSuffixes[i - 1] ? "block" : "none";
      sourceList.appendChild(el);
    }
  }

  document.addEventListener("click", e => {
    const clickedItem = e.target.closest("#list-suffix li");
    if (!clickedItem) return;
    if (clickedItem.style.opacity === "0.3") return;
    const object = wordObj.image.match(/\/([^/]+)\./)[1];

    const suffixText = clickedItem.querySelector("span")?.textContent.trim();
    if (!suffixText) return;

    /* ❌ WRONG */
    if (!correctSuffixes.includes(suffixText)) {
      clickedItem.classList.add("wrong-answer", "shake");
      setTimeout(() => {
        clickedItem.classList.remove("wrong-answer", "shake");
      }, 500);
      return;
    }

    /* ✅ CORRECT */
    const emptySlot = slots.find(s => s.dataset.full === "false");
    if (!emptySlot) return;

    selectedSuffixes.push(suffixText);
    handleSmoothMove(clickedItem, emptySlot);

    const rootWord = wordSpan.textContent.trim();
    const suffixString = selectedSuffixes.join("");
    const targetLength = rootWord.length + suffixString.length;

    // Find the answer that ends with the compiled suffixString and has length matching within 2 characters.
    let combined = answers.find(a => a.endsWith(suffixString) && Math.abs(a.length - targetLength) <= 2);

    // Fallback if not found 
    if (!combined) {
      combined = answers.find(a => a === rootWord + suffixString);
    }

    // If we didn't find any valid answer combination
    if (!combined || !answers.includes(combined)) {
      // alert("Wrong word formed");
      clickedItem.classList.add("wrong-answer", "shake");
      setTimeout(() => {
        clickedItem.classList.remove("wrong-answer", "shake");
      }, 500);

      // Revert the UI move
      const emptySlot = slots.find(s => s.dataset.full === "true" && s.querySelector('span')?.textContent === suffixText);
      if (emptySlot) {
        emptySlot.style.display = "none";
        emptySlot.dataset.full = "false";
        const clone = emptySlot.querySelector(".centerTxt-wrap");
        if (clone) clone.remove();
      }
      clickedItem.style.opacity = "1";
      clickedItem.style.pointerEvents = "auto";
      selectedSuffixes.pop();
      return;
    }

    // If already completed this exact answer, show alert
    if (completedAnswers.includes(combined)) {
      //alert(`You have already formed "${combined}". Try making a new word!`);
      const popUp = document.getElementById("wordAlertPopUp");
      popUp.style.display = "block";
      setTimeout(() => {
        popUp.style.display = "none";
      }, 1000);
      const canExtendRemaining = answers.some(a => a.startsWith(combined) && a !== combined && !completedAnswers.includes(a));

      if (!canExtendRemaining) {
        // Revert the UI move because it cannot form any new word
        const emptySlot = slots.find(s => s.dataset.full === "true" && s.querySelector('span')?.textContent === suffixText);
        if (emptySlot) {
          emptySlot.style.display = "none";
          emptySlot.dataset.full = "false";
          const clone = emptySlot.querySelector(".centerTxt-wrap");
          if (clone) clone.remove();
        }
        clickedItem.style.opacity = "1";
        clickedItem.style.pointerEvents = "auto";
        selectedSuffixes.pop();
      }
      return;
    }

    completedAnswers.push(combined);

    /* ---- ASSIGN TO word1, word2, ... ---- */
    if (wordSlots[wordIndex]) {
      wordSlots[wordIndex].textContent = combined;

      // Map object name to group container id and prefix used by foreignObjects
      const mapping = {
        tree: { container: 'treeWords', prefix: 'treeWord' },
        milestone: { container: 'milestoneWords', prefix: 'milestoneWord' },
        mountain: { container: 'mountainWords', prefix: 'mountainWord' },
        stone: { container: 'stoneObjects', prefix: 'stoneWord' }
      };

      const map = mapping[object];
      if (map) {
        // show group container
        const groupEl = document.getElementById(map.container);
        if (groupEl) groupEl.style.display = 'block';

        // show & populate the specific foreignObject slot (e.g., treeWord2)
        const slotId = `${map.prefix}${wordIndex + 1}`;
        const fo = document.getElementById(slotId);
        if (fo) {
          setTimeout(() => {
            fo.style.display = 'block';
            const span = fo.querySelector('span');
            if (span) span.textContent = combined;
          }, 1500)

        }
      }

      // ensure the lottie container for this leaf is visible and play animation
      const lottieContainer = document.getElementById(`${object}-${wordIndex}`);
      if (lottieContainer) lottieContainer.style.display = 'block';

      // populate any element with id wordN (tspan or span) as a fallback
      const genericWordEl = document.getElementById(`word${wordIndex + 1}`);
      if (genericWordEl) genericWordEl.textContent = combined;

      playCorrectAnswerLottie(object);
      wordIndex++;
      showExample.style.display = 'block';
    }

    const remainingSuffixes = correctSuffixes.filter(
      s => !selectedSuffixes.includes(s)
    );

    const canExtend = remainingSuffixes.some(
      suf => answers.includes(combined + suf)
    );

    setTimeout(() => {
      finalWord.style.display = "block";
      finalWord.querySelector("span").textContent = combined;

      // NOT last answer → hide & continue
      if (completedAnswers.length < answers.length) {
        setTimeout(() => {
          finalWord.style.display = "none";
        }, 1000);

        // Check if there are still any answers left that could be formed with the CURRENT suffix that was just placed
        const remainingAnswers = answers.filter(a => !completedAnswers.includes(a));
        const canStillUseSuffix = remainingAnswers.some(a => {
          // We approximate checking if the suffix is used. A more robust way could be derived if needed.
          // E.g., if one remaining answer is "harmlessly", and suffix was "less", does remaining contain it?

          // Based on your data structure, one root e.g. "harm" and correct suffixes ["ful", "ly", "less"]. 
          // If we just clicked "less" (forming harmless), is there another answer that uses "less"? 
          // Yes, "harmlessly" uses "less" implicitly (harm + less + ly).
          // Alternatively we can just check if any remaining answer includes this text
          return a.includes(suffixText);
        });

        if (canStillUseSuffix) {
          clickedItem.style.opacity = "1";
          clickedItem.style.pointerEvents = "auto";
        }
      }
      // LAST answer → lock puzzle
      else {
        document.querySelectorAll("#list-suffix li").forEach(li => {
          li.style.pointerEvents = "none";
          li.style.opacity = "0.3";
          showExample.style.display = 'block';
        });
      }

      setTimeout(() => {
        slots.forEach(slot => {
          slot.style.display = "none";
          slot.dataset.full = "false";
          const clone = slot.querySelector(".centerTxt-wrap");
          if (clone) clone.remove();
        });
        selectedSuffixes = [];
      }, 1000);

    }, 1000);
  });

  showExample.addEventListener('click', () => {
    showExampleSentences(wordObj);
    exampleSentence.style.display = 'block';
  });

  document.getElementById('closeExampleBtn').addEventListener('click', () => {
    // document.getElementById('examplePopup');
    exampleSentence.style.display = 'none';
  });


  if (nextBtn) nextBtn.addEventListener("click", loadNextWord);

  loadNextWord();
});