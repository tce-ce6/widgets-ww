Folder to refer and implement the code in:

wg82-commonly-confused-words

Develop an interactive SVG/HTML/JavaScript implementation based on the wireframe provided in the Adobe XD link below:
https://xd.adobe.com/view/6046bdab-37e2-4e3f-a4f5-5cef59d22de4-3515/
also the physical xd: 

Use the provided en_en_5_wg82.docx as the storyboard reference to design the interaction flow and define the functional logic.

Do not modify the existing UI layout or design inside the SVG in index.html, as this is the final, designer-approved layout.
Maintain the exact UI as per the wireframe and implement all required interactions strictly by showing and hiding SVG elements.

Treat index.html as the main boilerplate file containing the SVG layout.

Create a separate JavaScript file(js/script.js) that:

* Follows a function-based structure
* Stores all global variables inside a single global object
* Uses plain (vanilla) JavaScript
* Includes clear, explainable comments throughout the code
